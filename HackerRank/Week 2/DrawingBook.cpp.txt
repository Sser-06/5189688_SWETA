#include<iostream>
using namespace std;

int pageCount(int n, int p){
    int fromFront = p/2;
    int fromBack =(n/2)- (p/2);
    return min(fromFront, fromBack);
}

int main(){
    int n,p;
     cin>>n>>p;
     int result = pageCount(n,p);
     cout<<result<<endl;
     return 0;
}